import { Steps, Step } from "@/components/ui/steps";
import { BoxIcon } from "lucide-react";
import CodeSnippet from "@/components/docs-ui/code-snippet";
import { CodeBlock, FileName, Code } from "@/components/docs-ui/codeblock";

export const metadata = {
  title: "Install Nextjs 15",
  description: "Install Nextjs 15 to get started.",
  date: "2025-10-14",
  tags: ["Next.js", "Typescript"],
};

## Install Next.js 15

<Steps>
  <Step title="Create a new project">
    <CodeSnippet
  commands={[
    { label: "npm", icon: "BoxIcon", code: "npx create-next-app@latest" },
  ]}
/>
  </Step>

<Step title="On installation, you'll see the following prompts:"></Step>

  <Step title="Install dependencies">
    Navigate to your project directory and install the dependencies.
  </Step>
</Steps>
